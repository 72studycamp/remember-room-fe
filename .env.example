# Browser-visible backend base URL.
# Set this in Vercel Project Settings > Environment Variables for Production/Preview.
VITE_API_BASE_URL=http://3.39.6.80

# Local Vite-only proxy target. Used when VITE_API_BASE_URL is empty during local development.
VITE_DEV_API_PROXY_TARGET=http://localhost:8080
